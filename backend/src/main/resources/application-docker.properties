# ==========================================
# Docker application properties
# Overrides for running inside Docker Compose
# ==========================================
server.port=8080

# MySQL inside Docker network
spring.datasource.url=jdbc:mysql://mysql:3306/talent_connect_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=talent_connect_2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=TalentConnectSecretKeyForJWTTokenGeneration2026SecureRandomString
app.jwt.expiration-ms=86400000

# Email / SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sagar.pujari.developer@gmail.com
spring.mail.password=lkot bprw wffr vpmf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=/app/uploads/resumes

# Logging
logging.level.com.talentconnect=INFO
